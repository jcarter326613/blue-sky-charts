service: adds-data-collection
provider:
  name: aws
  runtime: nodejs12.x

functions:
  collect:
    handler: src/main/index.handler
    events:
      - http: GET handler

package:
  exclude:
    - src/*.ts
    - src/test
    - package.json
    - package-lock.json
    - README.md
    - tsconfig.json
    - compile
    - deploy